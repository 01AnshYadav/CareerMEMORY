# Local development only. Copy to .env.local.
# In production, set NEXT_PUBLIC_API_URL to your deployed backend URL via
# Vercel's environment variables (it is baked into the client bundle at build time).
NEXT_PUBLIC_API_URL=http://localhost:8000